type Menu {
  id: ID!
  creationDate: DateTime!
  modificationDate: DateTime!
  imageUrl: String!
  name: String!
  price: Int!
  likeCount: Int!
  likeRatio: Int!
  reviewCount: Int!
  orderCount: Int!
  reorderRatio: Int!
  hashtags: [String!]
  bookmark: Boolean!
}
