input MenuCreationInput {
  name: String!
  price: Int!
  category: String!
  storeId: ID!

  imageUrls: [String!]
  hashtags: [String!]
}

type Mutation {
  createMenu(input: MenuCreationInput!): ID!
}
