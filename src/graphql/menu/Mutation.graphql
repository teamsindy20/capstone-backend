input MenuCreationInput {
  name: String!
  price: Int!
  category: String!
  storeId: ID!

  # nullable
  imageUrls: [URL!]
  hashtags: [String!]
}

type Mutation {
  createMenu(input: MenuCreationInput!): ID!
}
